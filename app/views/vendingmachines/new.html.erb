<h1>Add a new vending machine</h1>

<!-- this is a HTML file with embedded Ruby code to create a form to capture user input
      I partitioned the form with 'divs'into three main parts to suit the UC1 SFR steps
      and offered to cancel the process at for SFR6 step 3a-->
<%= form_for @vendingmachine do |f| %>
         <div>
           <%= f.label :title %> 
           <%= f.text_field :title, placeholder: "vending machine's name", autofocus: true %>
         </div>

        <div>
          <%= f.label :configuration %>
          <%= f.text_field :configuration, rows:5, placeholder: "configuration of vending machine" %>
        </div>
        
        <div>
          <%= f.label :Factory_Identification_number %>
          <%= f.text_field :factory_id, placeholder: "enter factory ID" %>
        </div>
      
        <div>
          <!-- Asking confirmation from user for SFR -->
          <%= f.submit :onclick =>"return confirm('Sure')", class: "btn btn-primary btn-lg" %>
        </div>
        
      
    <% end %>